method1:

1. To prevent the exception in the 2D array, we can do this regular way:
    if (board == null || board.length == 0) return;
      int m = board.length, n = board[0].length;

method2:

It is a smart way to save space. 
