method1:

1. When I use coverage with int type, I can use boolean type instead.  boolean[] bold = new boolean[s.length()];
2. StringBuilder is a good way to create a string. 
3. Common ways to return a string:
  a. return res.toString(); // StringBuilder res = new StringBuilder();
  b. return String.valueOf(coverage);  
  c. return Arrays.toString(coverage); // int[] intArray = { 1, 2, 3, 4 ,5 }; String str = Arrays. toString(intArray)
