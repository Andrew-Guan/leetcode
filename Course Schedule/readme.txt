method1:

This questions actually is to check whether there is a loop in directed graph(it can be connected or disconnected). 
We can use an easy solution (white set, grey set, black set) to solve it. 
It use dfs to traverse all the node with recursion function. 
