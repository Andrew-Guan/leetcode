method1:

This method costs too much time. It takes 582ms. 

method2:

This method is much faster than the first one. It costs 22ms.
Just try to get the first index and last index by using string.indexOf() and string.lastIndexOf().
